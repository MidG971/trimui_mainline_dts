build/
*.dtb
.DS_Store
*.7z
kernel-source/
*.swp
test_format.dts
include

# U-Boot / firmware build artifacts — never commit binaries here
*.bin
*.img
*.awimg
*.fex
uboot/vendor-boot0/
u-boot-sunxi-with-spl*

# Allwinner Confidential docs — NEVER commit (datasheet + register user manual)
*.pdf
a523_trm.pdf
A523_User_Manual*
.aider*

# Unsent outreach draft — keep local, send directly on GitHub (do not publish here)
docs/UPSTREAM-COLLAB-DRAFT.md
docs/CHENYU-CODEC-FEEDBACK-DRAFT.md

# Local device captures — never commit (contain serial/MAC/IP)
*.log
recon-*.log
vendor-dmesg-*.log
bringup/
live.dts
live.dtb
